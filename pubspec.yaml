name: pigment
description: Useful and simple flutter color handler.
version: 2.0.0
homepage: https://github.com/bregydoc/pigment

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
